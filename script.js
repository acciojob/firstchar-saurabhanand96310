function firstChar(s) {
  // your code here
	s=s.trimm();
	let p=""
	if(s.indexOf(0)!=0){
		return s.indexOf(0);
	}else{
		return p;
	}
}

// Do not change the code below

//const text = prompt("Enter text:");
//alert(firstChar(text));
